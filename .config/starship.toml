add_newline = false

format = """[┏━](fg:red) $username@$hostname$directory$git_branch$git_commit$git_state$git_metrics$git_status$fill${custom.hack}$memory_usage$jobs[━┓](fg:red)
[┗━](fg:red) $status$character"""
right_format = "$cmd_duration$localip[━┛](fg:red)"
palette = "kanagawa"

[palettes.kanagawa]
red = "#c4746e"
yellow = "#c4b28a"
green = "#87a987"
bright-green = "#8a9a7b"
background = "#1D1C19"

# so, for some reason, if there is *not* a % in the prompt, the funny unicode symbols won't align 🤨
# computers were a mistake
# i just noticed it because i had the battery widget... how lucky...
[custom.hack]
symbol = "[%](fg:background)"
when = true

[directory]
truncation_length = 3

[localip]
ssh_only = false
disabled = false
style = "fg:yellow"

[cmd_duration]
min_time = 1_000
show_milliseconds = false

[fill]
# symbol = "━"
symbol = " "
style = "yellow"

[character]
success_symbol = '[󱐋](yellow)'
error_symbol = '[󱐋](red)'
vicmd_symbol = '[󱐋](green)'

[[battery.display]]
threshold = 100

[memory_usage]
disabled = false
threshold = 1
format = "[${ram}]($style) "

[time]
disabled = false
use_12hr = true
style = "fg:yellow"

[username]
show_always = true
style_user = "fg:yellow"
style_root = "fg:yellow"
format = '[$user]($style)'

[hostname]
disabled = false
ssh_only = false
format = '[$hostname]($style) in '

[ruby]
detect_variables = ["RENV_VERSION"]
symbol = " "

[status]
disabled = false
symbol = ""

[battery]
full_symbol = "󰁹 "
charging_symbol = "󰂄 "
discharging_symbol = "󰁾 "
unknown_symbol = "󰂑 "
empty_symbol = "󰂎 "
